// Require modules
var express = require('express');

// Global vriables
var db = require('../models');
var router = express.Router(); // tell it that theis is a router


// Define routes-remember they are relative to /poke
