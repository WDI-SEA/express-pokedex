<div class="container">
  <h1 class="text-center">My Favorite Pokemon</h1>
<p class="text-center">
  <a href="/" class="btn btn-info">Home</a>
</p>
<% if(pokemon.length <=0) { %>
  <h2>No Pokemon have been added!</h2>
<%} else {%>
  <% pokemon.forEach(function(pokemon){%>
  <div class="well">
    <h2><%= pokemon.name %></h2>
    <form class="delete-fav" action="./pokemon/<%= pokemon.id %>">
      <button class="btn btn-danger glyphicon glyphicon-trash" type="submit"></button>
    </form>
  </div>
<% }); %>
<%}%>
</div>
