<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>My Fav Poke</title>
</head>
<body>
    <h2>My Favorite Poke</h2>
    <% if(poke.length) { %>
        <% poke.forEach((poke) => { %>
            <div>
                <p>Name: <%= poke.name %></p>
            </div>
        <% }) %>
    <% } else { %>
        <p>No favorite Poke yet!</p>
    <% } %>
</body>
</html>

