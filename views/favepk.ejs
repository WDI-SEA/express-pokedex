<!--Show a list of PK mon saved from API and into the DB-->
<h1>Pokedex: Favorites</h1>

<div class="favorites">
<% pokemon.forEach(function(pokemon) { %>
	<p>
		<a href="/pokemon/<%= pokemon.id %>">
			<%= pokemon.name %>
		</a>
		</p>
<% }); %>
</div>
<a href="/">Back to Pokemon</a>



