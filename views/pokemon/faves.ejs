<main class="container">
  <h1 class="text-center">Favorites</h1>
  <nav>
    <a href="/">Home</a>
  </nav>

  <section class="collection">
    <% faves.forEach(function(pokemon) { %>
      <a href="/pokemon/<%=pokemon.name%>" class="poke-link well">
        <div>
          <h2><%= pokemon.name%></h2>
          
          <form method="POST" action="/pokemon/<%= pokemon.name %>?_method=DELETE">
            <input
              hidden
              type="text"
              name="name"
              value="<%= pokemon.name %>"
            />
            <button class="index-fave-btn fave-btn" type="submit">
              <img src="/img/fave-delete.svg" alt="">
            </button>
          </form>
          
        </div>
      </a>
    <% }); %>
  </section>
</main>
  