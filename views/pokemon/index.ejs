<div class="container">
  <% pokemons.forEach((pokemon) => { %>
      <div class="well">
        <h2><%= pokemon.name %></h2>
        <a href="/pokemon/<%= pokemon.name %>" class="btn btn-primary">More Info</a>
      </div>
  <% }); %>
</div>