<div class="container">
    <h1 class="text-center">Favorite Pokemon</h1>
    <p class="text-center">
      <a href="/">Home</a>
    </p>
    <% pokemon.forEach((poke, index) => { %>
    <div class="well">
        <h2> <a href="pokemon/<%= index %>/<%= poke.name %>"><%= poke.name %></a></h2>
    </div>
    <% }); %>

