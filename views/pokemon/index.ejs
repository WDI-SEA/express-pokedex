<div class="container">
    <h1 class="text-center">Favorite Pokemon</h1>
    <p class="text-center">
      <a href="/">Home</a>
    </p>
  
  
    <% pokemon.forEach((poke, index) => { %>
    <%  console.log(poke.name) %>
    <% }); %>

