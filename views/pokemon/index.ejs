<h1>Favorite Pokemon</h1>
<% favePokes.forEach(fave => { %>
   <a href="/pokemon/<%= fave.name %>"> <h2><%= fave.name %> </h2></a>
    <form method="POST" action="/pokemon/<%= fave.name %>/?_method=DELETE">
        <button type="submit">Delete</button>
    </form>
<% }) %> 
