
    <h1>My Favorite Pokemon</h1>

    <% result.forEach(function(pokemon) {%>
    <div class ="well">
    <h3><%= pokemon.name %></h3>
     <a href="/pokemon/<%-pokemon.name %>" class="btn btn-primary">Get Info</a>
    </div>
    <% });%>
