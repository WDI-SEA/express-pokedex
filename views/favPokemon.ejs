
<div class="list">
  <h2 class="btn btn-primary favPoke" role="button"><a href="<%= "/" %>"> Back to All Pokemon</a></h2>

   <% result.forEach(function(pokemon) {%>
   <div class ="pokemon">
     <h3><%= pokemon.name %></h3>
     <a href="/pokemon/<%-pokemon.name %>" class="btn btn-primary">Stats</a>
   </div>
   <% });%>
</div>
