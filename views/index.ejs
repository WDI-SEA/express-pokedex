<!DOCTYPE html>
<%- include ./partials/header.ejs %>
  <h1>The Original 151 Pokémon</h1>
  <div class="main-gallery">
      <% pokemon.forEach(function(pokemon) { %>
              <div class="card yellow darken-1 gallery-cell">
                <div class="card-content white-text">
                  <span class="card-title"><%= pokemon.name %></span>
                   <form id='sub' method="POST" action="/pokemon">
                     <input hidden type="text" name="name" value="<%= pokemon.name %>">
                     <button type="submit"><img class = 'pokeball responsive-img' src='https://png.icons8.com/color/1600/pokeball-2.png'></button>
                     <p id='catch'>Catch 'em!</p>
                     <a id='deets' href="./pokemon/<%= pokemon.name %>">View Details</a>
                   </form>
                </div>
              </div>
      <% }); %>

<%- include ./partials/footer.ejs %>

