<!DOCTYPE html>

<head>
    <title>Favorite Pokemon</title>
</head>
<body>
    <div class="container text-center">
    <h1>My favorite Pokemon!</h1>
    <% if (pokemons.length) { %>
        <% pokemons.forEach(poke => { %>
            <div class="container">
                <h2>Pokemon: <%= poke.name %></h2>
                
                <a class="btn btn-default"href="favorites/<%- poke.id %>">Details</a>
                <hr>
            </div>
        <% }) %>
    <% } else { %>
        <h2>Add some favorites to view interesting PokeFacts!</h2>
    <% } %>
    </div>
</body>
</html>