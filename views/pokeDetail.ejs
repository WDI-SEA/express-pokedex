<h2><%= name%></h2>

<img src ="<%=image%>" alt="pic of poke"

<h4> list of abilities </h4>
<ul>
<% abils.forEach(ability => {%>
    <li><%=ability.ability.name %></li>
    <%})%>
</ul>

<h4> list of moves </h4>
<ul>
<% moves.forEach(move => {%>
    <li><%=move.move.name %></li>
    <%})%>
</ul>


<h4> stats </h4>
<ul>
<% stats.forEach(stat => {%>
    <li><%=stat.stat.name %></li>
    <%})%>
</ul>